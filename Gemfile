source 'http://rubygems.org'
ruby '2.1.5'

# Gems removed from Rails 4, but added here for legacy support
gem 'rails-observers'
gem 'acts_as_list'
gem 'protected_attributes'

# Use jquery
gem 'jquery-rails'

# Use puma webserver
gem 'puma'

gem 'rails'
gem 'pg'
gem 'devise'

group :development, :test do
  gem 'rspec-rails'
  gem 'rspec-activemodel-mocks'
  gem 'taps'
end

group :test do
  gem 'timecop'
end
